/* Hand-written: the bundle is vendored JavaScript, so tsc emits no declaration for it. */
import type { Plugin } from "@oxlint/plugins";

declare const antiSlopPlugin: Plugin;

export default antiSlopPlugin;
