---
"oxlint-config-awesomeness": minor
---

Turn off `one-var`. The rule ships in the `style` category we bulk-enable, so it is now pinned to `"off"` rather than removed, which would restore the upstream `"always"` default.
