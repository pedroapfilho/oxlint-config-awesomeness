---
"oxlint-config-awesomeness": patch
---

Ship type declarations for the `oxlint-config-awesomeness/anti-slop` export. The vendored bundle is JavaScript, so `tsc` emitted nothing for it and importing the subpath from a TypeScript config resolved to an untyped module.
